import { Foo, Bar, ExampleComponent } from '.'

describe('ExampleComponent', () => {
  it('is truthy', () => {
    expect(ExampleComponent).toBeTruthy()
  })
})

describe('Foo', () => {
  it('is truthy', () => {
    expect(Foo).toBeTruthy()
  })
})

describe('Bar', () => {
  it('is truthy', () => {
    expect(Bar).toBeTruthy()
  })
})
