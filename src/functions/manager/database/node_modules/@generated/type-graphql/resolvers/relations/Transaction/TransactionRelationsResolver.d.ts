import { Manager } from "../../../models/Manager";
import { PAT } from "../../../models/PAT";
import { Transaction } from "../../../models/Transaction";
export declare class TransactionRelationsResolver {
    responsibleManager(transaction: Transaction, ctx: any): Promise<Manager>;
    pat(transaction: Transaction, ctx: any): Promise<PAT | null>;
}
